# Runtime dependencies for the hosted FastAPI backend (app/main.py).
#
# These mirror the runtime + "web" extra in pyproject.toml. We list the
# dependencies directly (rather than installing the project package) on purpose:
# the backend runs straight from the repo checkout, so app/main.py keeps the
# repo root as PROJECT_ROOT and can find tracks/, data/ and cache/ at runtime.
#
# Used by free hosts (e.g. Render) via:  pip install -r requirements.txt

# --- analysis pipeline (src/) ---
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
matplotlib==3.10.9
fastf1==3.8.3

# --- web layer (app/) ---
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.29
